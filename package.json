{
  "name": "llm-data-core",
  "version": "1.0.0",
  "description": "Layer-3 Core Integration Bundle for LLM data coordination",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "data-core": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "node --test dist/tests/*.js",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "claude-flow": "^2.7.47"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "keywords": [
    "llm",
    "data-core",
    "integration",
    "layer-3"
  ],
  "license": "UNLICENSED"
}
